BUILDING THE USER'S GUIDE

Install Prerequisites
1.  Install R packages:  bookdown
2.  Install on OS and ensure on PATH: pandoc, pandoc-citeproc,
pdflatex

Make Documentation
> cd <stan>
> make doc
> ls doc

The doc in both HTML and PDF form will be laid out in the
<stan>/doc directory.
