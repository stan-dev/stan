diff --git a/src/docs/stan-reference/commands.tex b/src/docs/stan-reference/commands.tex
index 0c384fe..32062bf 100644
--- a/src/docs/stan-reference/commands.tex
+++ b/src/docs/stan-reference/commands.tex
@@ -777,7 +777,7 @@ samples1.csv	samples2.csv	samples3.csv
 \end{Verbatim}
 {\small
 \begin{Verbatim}[fontshape=sl]
-\A{Inference for Stan model:} \FXA \  
+Inference for Stan model:
 3 chains: each with iter=(1000,1000,1000); warmup=(0,0,0); 
 thin=(1,1,1); 3000 iterations saved.
 
@@ -785,9 +785,9 @@ thin=(1,1,1); 3000 iterations saved.
 alpha  16.2     2.0 41.5  1.4  5.2  9.7 17.6  59.2   435  1.0
 beta   10.0     1.1 22.8  0.8  3.3  6.2 11.2  37.5   405  1.0
 
-\A{Samples were drawn using mcmc::nuts_diag.} \FXA \ 
-\A{For each parameter, n_eff is a crude measure of effective} \FXA \  
-\A{sample size, and Rhat is the potential scale reduction factor} \FXA \  
+Samples were drawn using mcmc::nuts_diag.
+For each parameter, n_eff is a crude measure of effective
+sample size, and Rhat is the potential scale reduction factor
 on split chains (at convergence, Rhat=1).
 \end{Verbatim}
 }
diff --git a/src/docs/stan-reference/introduction.tex b/src/docs/stan-reference/introduction.tex
index 3661b60..340e80e 100644
--- a/src/docs/stan-reference/introduction.tex
+++ b/src/docs/stan-reference/introduction.tex
@@ -566,16 +566,16 @@ run. To run \code{print} on the output file generated for
 \begin{quote}
 {\small
 \begin{Verbatim}[fontshape=sl]
-\A{Inference for Stan model:} \FXA \ 
+Inference for Stan model:
 1 chains: each with iter=(1000); warmup=(0); thin=(1); 
 1000 iterations saved.
 
        mean se_mean   sd 2.5%  25%  50%  75% 97.5% n_eff Rhat
 theta   0.2     0.0  0.1  0.1  0.2  0.2  0.3   0.5   478  1.0
 
-\A{Samples were drawn using mcmc::nuts_diag.} \FXA \ 
-\A{For each parameter, n_eff is a crude measure of effective} \FXA \  
-\A{sample size, and Rhat is the potential scale reduction factor} \FXA \  
+Samples were drawn using mcmc::nuts_diag.
+For each parameter, n_eff is a crude measure of effective
+sample size, and Rhat is the potential scale reduction factor
 on split chains (at convergence, Rhat=1).
 \end{Verbatim}
 }
diff --git a/src/docs/stan-reference/programming.tex b/src/docs/stan-reference/programming.tex
index 147b87f..612fb85 100644
--- a/src/docs/stan-reference/programming.tex
+++ b/src/docs/stan-reference/programming.tex
@@ -2378,7 +2378,7 @@ for the covariance matrix \code{Sigma} may be encoded as follows.
 %
 \begin{quote}
 \begin{Verbatim}
-\A{... data block as before, but without alpha ...} \FXA \ 
+... data block as before, but without alpha ...
 parameters {
   vector[K] mu;              // topic mean
   corr_matrix[K] Omega;      // correlation matrix
