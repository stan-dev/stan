# Compound Arithmetic and Assignment

Compound arithmetic and assignment statements combine an arithmetic
operation and assignment,

```
 x = x op y;
 ```

replacing them with the compound form

```
 x op= y;
 ```

For example, `x = x + 1` may be replaced with `x += 1`.

The signatures of the supported compound arithmetic and assignment
operations are as follows.

## Compound Addition and Assignment

<!-- void; operator+=; (int x, int y); -->
\index{{\tt \bfseries operator\_compound\_add}!(int x, int y): void|hyperpage}

`void` **`operator+=`**(`int x, int y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (real x, real y); -->
\index{{\tt \bfseries operator\_compound\_add}!(real x, real y): void|hyperpage}

`void` **`operator+=`**(`real x, real y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_add}!(vector x, real y): void|hyperpage}

`void` **`operator+=`**(`vector x, real y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (row_vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_add}!(row\_vector x, real y): void|hyperpage}

`void` **`operator+=`**(`row_vector x, real y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (matrix x, real y); -->
\index{{\tt \bfseries operator\_compound\_add}!(matrix x, real y): void|hyperpage}

`void` **`operator+=`**(`matrix x, real y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (vector x, vector y); -->
\index{{\tt \bfseries operator\_compound\_add}!(vector x, vector y): void|hyperpage}

`void` **`operator+=`**(`vector x, vector y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (row_vector x, row_vector y); -->
\index{{\tt \bfseries operator\_compound\_add}!(row\_vector x, row\_vector y): void|hyperpage}

`void` **`operator+=`**(`row_vector x, row_vector y`)
`x += y` is equivalent to `x = x + y`.

<!-- void; operator+=; (matrix x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_add}!(matrix x, matrix y): void|hyperpage}

`void` **`operator+=`**(`matrix x, matrix y`)
`x += y` is equivalent to `x = x + y`.

## Compound Subtraction and Assignment

<!-- void; operator-=; (int x, int y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(int x, int y): void|hyperpage}

`void` **`operator-=`**(`int x, int y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (real x, real y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(real x, real y): void|hyperpage}

`void` **`operator-=`**(`real x, real y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(vector x, real y): void|hyperpage}

`void` **`operator-=`**(`vector x, real y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (row_vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(row\_vector x, real y): void|hyperpage}

`void` **`operator-=`**(`row_vector x, real y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (matrix x, real y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(matrix x, real y): void|hyperpage}

`void` **`operator-=`**(`matrix x, real y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (vector x, vector y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(vector x, vector y): void|hyperpage}

`void` **`operator-=`**(`vector x, vector y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (row_vector x, row_vector y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(row\_vector x, row\_vector y): void|hyperpage}

`void` **`operator-=`**(`row_vector x, row_vector y`)
`x -= y` is equivalent to `x = x - y`.

<!-- void; operator-=; (matrix x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_subtract}!(matrix x, matrix y): void|hyperpage}

`void` **`operator-=`**(`matrix x, matrix y`)
`x -= y` is equivalent to `x = x - y`.

## Compound Multiplication and Assignment

<!-- void; operator*=; (int x, int y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(int x, int y): void|hyperpage}

`void` **`operator*=`**(`int x, int y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (real x, real y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(real x, real y): void|hyperpage}

`void` **`operator*=`**(`real x, real y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(vector x, real y): void|hyperpage}

`void` **`operator*=`**(`vector x, real y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (row_vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(row\_vector x, real y): void|hyperpage}

`void` **`operator*=`**(`row_vector x, real y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (matrix x, real y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(matrix x, real y): void|hyperpage}

`void` **`operator*=`**(`matrix x, real y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (row_vector x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(row\_vector x, matrix y): void|hyperpage}

`void` **`operator*=`**(`row_vector x, matrix y`)
`x *= y` is equivalent to `x = x * y`.

<!-- void; operator*=; (matrix x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_multiply}!(matrix x, matrix y): void|hyperpage}

`void` **`operator*=`**(`matrix x, matrix y`)
`x *= y` is equivalent to `x = x * y`.

## Compound Division and Assignment

<!-- void; operator/=; (int x, int y); -->
\index{{\tt \bfseries operator\_compound\_divide}!(int x, int y): void|hyperpage}

`void` **`operator/=`**(`int x, int y`)
`x /= y` is equivalent to `x = x / y`.

<!-- void; operator/=; (real x, real y); -->
\index{{\tt \bfseries operator\_compound\_divide}!(real x, real y): void|hyperpage}

`void` **`operator/=`**(`real x, real y`)
`x /= y` is equivalent to `x = x / y`.

<!-- void; operator/=; (vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_divide}!(vector x, real y): void|hyperpage}

`void` **`operator/=`**(`vector x, real y`)
`x /= y` is equivalent to `x = x / y`.

<!-- void; operator/=; (row_vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_divide}!(row\_vector x, real y): void|hyperpage}

`void` **`operator/=`**(`row_vector x, real y`)
`x /= y` is equivalent to `x = x / y`.

<!-- void; operator/=; (matrix x, real y); -->
\index{{\tt \bfseries operator\_compound\_divide}!(matrix x, real y): void|hyperpage}

`void` **`operator/=`**(`matrix x, real y`)
`x /= y` is equivalent to `x = x / y`.

## Compound Elementwise Multiplication and Assignment

<!-- void; operator.*=; (vector x, vector y); -->
\index{{\tt \bfseries operator\_compound\_elt\_multiply}!(vector x, vector y): void|hyperpage}

`void` **`operator.*=`**(`vector x, vector y`)
`x .*= y` is equivalent to `x = x .* y`.

<!-- void; operator.*=; (row_vector x, row_vector y); -->
\index{{\tt \bfseries operator\_compound\_elt\_multiply}!(row\_vector x, row\_vector y): void|hyperpage}

`void` **`operator.*=`**(`row_vector x, row_vector y`)
`x .*= y` is equivalent to `x = x .* y`.

<!-- void; operator.*=; (matrix x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_elt\_multiply}!(matrix x, matrix y): void|hyperpage}

`void` **`operator.*=`**(`matrix x, matrix y`)
`x .*= y` is equivalent to `x = x .* y`.

## Compound Elementwise Division and Assignment

<!-- void; operator./=; (vector x, vector y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(vector x, vector y): void|hyperpage}

`void` **`operator./=`**(`vector x, vector y`)
`x ./= y` is equivalent to `x = x ./ y`.

<!-- void; operator./=; (row_vector x, row_vector y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(row\_vector x, row\_vector y): void|hyperpage}

`void` **`operator./=`**(`row_vector x, row_vector y`)
`x ./= y` is equivalent to `x = x ./ y`.

<!-- void; operator./=; (matrix x, matrix y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(matrix x, matrix y): void|hyperpage}

`void` **`operator./=`**(`matrix x, matrix y`)
`x ./= y` is equivalent to `x = x ./ y`.

<!-- void; operator./=; (vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(vector x, real y): void|hyperpage}

`void` **`operator./=`**(`vector x, real y`)
`x ./= y` is equivalent to `x = x ./ y`.

<!-- void; operator./=; (row_vector x, real y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(row\_vector x, real y): void|hyperpage}

`void` **`operator./=`**(`row_vector x, real y`)
`x ./= y` is equivalent to `x = x ./ y`.

<!-- void; operator./=; (matrix x, real y); -->
\index{{\tt \bfseries operator\_compound\_elt\_divide}!(matrix x, real y): void|hyperpage}

`void` **`operator./=`**(`matrix x, real y`)
`x ./= y` is equivalent to `x = x ./ y`.

