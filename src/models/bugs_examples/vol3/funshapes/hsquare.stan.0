# hollow square 

# not working, hanging 

parameters {
  real(-1, 1) x; 
  real(-1, 1) y; 
} 

model {
  // lp__ <- log(step(1 - x * x - y * y)); 
  lp__ <- lp__ + log(fmax(abs(x) - .5, 0));
  lp__ <- lp__ + log(fmax(abs(y) - .5, 0));
} 
