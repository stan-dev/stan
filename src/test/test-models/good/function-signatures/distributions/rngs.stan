parameters {
  real y;
}
model {
  y ~ normal(0,1);
}
generated quantities {
  int n;
  real z;
  real x[3];
  real u[3];
  vector[3] theta;
  vector[3] alpha;
  row_vector[3] nu;
  row_vector[3] nu_array[3];
  vector[3] v;
  vector[3] v_array[3];
  vector[3] mu;
  vector[3] mu_array[3];
  matrix[3,3] Sigma;
  matrix[3,3] L;
  int ns[3];
  int N[3];

  n = bernoulli_logit_rng(0.0);
  N = bernoulli_logit_rng(alpha);
  N = bernoulli_logit_rng(nu);
  N = bernoulli_logit_rng(ns);
  N = bernoulli_logit_rng(u);
  n = bernoulli_logit_rng(3);

  N = binomial_rng(ns, alpha);
  N = binomial_rng(ns, nu);
  N = binomial_rng(ns, ns);
  N = binomial_rng(ns, u);
  N = binomial_rng(ns, 3);
  N = binomial_rng(ns, 3.5);
  N = binomial_rng(3, alpha);
  N = binomial_rng(3, nu);
  N = binomial_rng(3, ns);
  N = binomial_rng(3, u);
  n = binomial_rng(3, 3);
  n = binomial_rng(3, 3.5);

  N = neg_binomial_2_log_rng(alpha, alpha);
  N = neg_binomial_2_log_rng(alpha, nu);
  N = neg_binomial_2_log_rng(alpha, ns);
  N = neg_binomial_2_log_rng(alpha, u);
  N = neg_binomial_2_log_rng(alpha, 3);
  N = neg_binomial_2_log_rng(alpha, 3.5);
  N = neg_binomial_2_log_rng(nu, alpha);
  N = neg_binomial_2_log_rng(nu, nu);
  N = neg_binomial_2_log_rng(nu, ns);
  N = neg_binomial_2_log_rng(nu, u);
  N = neg_binomial_2_log_rng(nu, 3);
  N = neg_binomial_2_log_rng(nu, 3.5);
  N = neg_binomial_2_log_rng(ns, alpha);
  N = neg_binomial_2_log_rng(ns, nu);
  N = neg_binomial_2_log_rng(ns, ns);
  N = neg_binomial_2_log_rng(ns, u);
  N = neg_binomial_2_log_rng(ns, 3);
  N = neg_binomial_2_log_rng(ns, 3.5);
  N = neg_binomial_2_log_rng(u, alpha);
  N = neg_binomial_2_log_rng(u, nu);
  N = neg_binomial_2_log_rng(u, ns);
  N = neg_binomial_2_log_rng(u, u);
  N = neg_binomial_2_log_rng(u, 3);
  N = neg_binomial_2_log_rng(u, 3.5);
  N = neg_binomial_2_log_rng(3, alpha);
  N = neg_binomial_2_log_rng(3, nu);
  N = neg_binomial_2_log_rng(3, ns);
  N = neg_binomial_2_log_rng(3, u);
  n = neg_binomial_2_log_rng(3, 3);
  n = neg_binomial_2_log_rng(3, 3.5);
  N = neg_binomial_2_log_rng(3.5, alpha);
  N = neg_binomial_2_log_rng(3.5, nu);
  N = neg_binomial_2_log_rng(3.5, ns);
  N = neg_binomial_2_log_rng(3.5, u);
  n = neg_binomial_2_log_rng(3.5, 3);
  n = neg_binomial_2_log_rng(3.5, 3.5);
  N = neg_binomial_2_rng(alpha, alpha);
  N = neg_binomial_2_rng(alpha, nu);
  N = neg_binomial_2_rng(alpha, ns);
  N = neg_binomial_2_rng(alpha, u);
  N = neg_binomial_2_rng(alpha, 3);
  N = neg_binomial_2_rng(alpha, 3.5);
  N = neg_binomial_2_rng(nu, alpha);
  N = neg_binomial_2_rng(nu, nu);
  N = neg_binomial_2_rng(nu, ns);
  N = neg_binomial_2_rng(nu, u);
  N = neg_binomial_2_rng(nu, 3);
  N = neg_binomial_2_rng(nu, 3.5);
  N = neg_binomial_2_rng(ns, alpha);
  N = neg_binomial_2_rng(ns, nu);
  N = neg_binomial_2_rng(ns, ns);
  N = neg_binomial_2_rng(ns, u);
  N = neg_binomial_2_rng(ns, 3);
  N = neg_binomial_2_rng(ns, 3.5);
  N = neg_binomial_2_rng(u, alpha);
  N = neg_binomial_2_rng(u, nu);
  N = neg_binomial_2_rng(u, ns);
  N = neg_binomial_2_rng(u, u);
  N = neg_binomial_2_rng(u, 3);
  N = neg_binomial_2_rng(u, 3.5);
  N = neg_binomial_2_rng(3, alpha);
  N = neg_binomial_2_rng(3, nu);
  N = neg_binomial_2_rng(3, ns);
  N = neg_binomial_2_rng(3, u);
  n = neg_binomial_2_rng(3, 3);
  n = neg_binomial_2_rng(3, 3.5);
  N = neg_binomial_2_rng(3.5, alpha);
  N = neg_binomial_2_rng(3.5, nu);
  N = neg_binomial_2_rng(3.5, ns);
  N = neg_binomial_2_rng(3.5, u);
  n = neg_binomial_2_rng(3.5, 3);
  n = neg_binomial_2_rng(3.5, 3.5);
  N = neg_binomial_rng(alpha, alpha);
  N = neg_binomial_rng(alpha, nu);
  N = neg_binomial_rng(alpha, ns);
  N = neg_binomial_rng(alpha, u);
  N = neg_binomial_rng(alpha, 3);
  N = neg_binomial_rng(alpha, 3.5);
  N = neg_binomial_rng(nu, alpha);
  N = neg_binomial_rng(nu, nu);
  N = neg_binomial_rng(nu, ns);
  N = neg_binomial_rng(nu, u);
  N = neg_binomial_rng(nu, 3);
  N = neg_binomial_rng(nu, 3.5);
  N = neg_binomial_rng(ns, alpha);
  N = neg_binomial_rng(ns, nu);
  N = neg_binomial_rng(ns, ns);
  N = neg_binomial_rng(ns, u);
  N = neg_binomial_rng(ns, 3);
  N = neg_binomial_rng(ns, 3.5);
  N = neg_binomial_rng(u, alpha);
  N = neg_binomial_rng(u, nu);
  N = neg_binomial_rng(u, ns);
  N = neg_binomial_rng(u, u);
  N = neg_binomial_rng(u, 3);
  N = neg_binomial_rng(u, 3.5);
  N = neg_binomial_rng(3, alpha);
  N = neg_binomial_rng(3, nu);
  N = neg_binomial_rng(3, ns);
  N = neg_binomial_rng(3, u);
  n = neg_binomial_rng(3, 3);
  n = neg_binomial_rng(3, 3.5);
  N = neg_binomial_rng(3.5, alpha);
  N = neg_binomial_rng(3.5, nu);
  N = neg_binomial_rng(3.5, ns);
  N = neg_binomial_rng(3.5, u);
  n = neg_binomial_rng(3.5, 3);
  n = neg_binomial_rng(3.5, 3.5);

  x = uniform_rng(alpha, alpha);
  x = uniform_rng(alpha, nu);
  x = uniform_rng(alpha, ns);
  x = uniform_rng(alpha, u);
  x = uniform_rng(alpha, 3);
  x = uniform_rng(alpha, 3.5);
  x = uniform_rng(nu, alpha);
  x = uniform_rng(nu, nu);
  x = uniform_rng(nu, ns);
  x = uniform_rng(nu, u);
  x = uniform_rng(nu, 3);
  x = uniform_rng(nu, 3.5);
  x = uniform_rng(ns, alpha);
  x = uniform_rng(ns, nu);
  x = uniform_rng(ns, ns);
  x = uniform_rng(ns, u);
  x = uniform_rng(ns, 3);
  x = uniform_rng(ns, 3.5);
  x = uniform_rng(u, alpha);
  x = uniform_rng(u, nu);
  x = uniform_rng(u, ns);
  x = uniform_rng(u, u);
  x = uniform_rng(u, 3);
  x = uniform_rng(u, 3.5);
  x = uniform_rng(3, alpha);
  x = uniform_rng(3, nu);
  x = uniform_rng(3, ns);
  x = uniform_rng(3, u);
  z = uniform_rng(3, 3);
  z = uniform_rng(3, 3.5);
  x = uniform_rng(3.5, alpha);
  x = uniform_rng(3.5, nu);
  x = uniform_rng(3.5, ns);
  x = uniform_rng(3.5, u);
  z = uniform_rng(3.5, 3);
  z = uniform_rng(3.5, 3.5);

  x = von_mises_rng(alpha, alpha);
  x = von_mises_rng(alpha, nu);
  x = von_mises_rng(alpha, ns);
  x = von_mises_rng(alpha, u);
  x = von_mises_rng(alpha, 3);
  x = von_mises_rng(alpha, 3.5);
  x = von_mises_rng(nu, alpha);
  x = von_mises_rng(nu, nu);
  x = von_mises_rng(nu, ns);
  x = von_mises_rng(nu, u);
  x = von_mises_rng(nu, 3);
  x = von_mises_rng(nu, 3.5);
  x = von_mises_rng(ns, alpha);
  x = von_mises_rng(ns, nu);
  x = von_mises_rng(ns, ns);
  x = von_mises_rng(ns, u);
  x = von_mises_rng(ns, 3);
  x = von_mises_rng(ns, 3.5);
  x = von_mises_rng(u, alpha);
  x = von_mises_rng(u, nu);
  x = von_mises_rng(u, ns);
  x = von_mises_rng(u, u);
  x = von_mises_rng(u, 3);
  x = von_mises_rng(u, 3.5);
  x = von_mises_rng(3, alpha);
  x = von_mises_rng(3, nu);
  x = von_mises_rng(3, ns);
  x = von_mises_rng(3, u);
  z = von_mises_rng(3, 3);
  z = von_mises_rng(3, 3.5);
  x = von_mises_rng(3.5, alpha);
  x = von_mises_rng(3.5, nu);
  x = von_mises_rng(3.5, ns);
  x = von_mises_rng(3.5, u);
  z = von_mises_rng(3.5, 3);
  z = von_mises_rng(3.5, 3.5);

  // n = binomial_logit_rng(15,-1.2);
  
  N = beta_binomial_rng(ns, alpha, alpha);
  N = beta_binomial_rng(ns, alpha, nu);
  N = beta_binomial_rng(ns, alpha, ns);
  N = beta_binomial_rng(ns, alpha, u);
  N = beta_binomial_rng(ns, alpha, 3);
  N = beta_binomial_rng(ns, alpha, 3.5);
  N = beta_binomial_rng(ns, nu, alpha);
  N = beta_binomial_rng(ns, nu, nu);
  N = beta_binomial_rng(ns, nu, ns);
  N = beta_binomial_rng(ns, nu, u);
  N = beta_binomial_rng(ns, nu, 3);
  N = beta_binomial_rng(ns, nu, 3.5);
  N = beta_binomial_rng(ns, ns, alpha);
  N = beta_binomial_rng(ns, ns, nu);
  N = beta_binomial_rng(ns, ns, ns);
  N = beta_binomial_rng(ns, ns, u);
  N = beta_binomial_rng(ns, ns, 3);
  N = beta_binomial_rng(ns, ns, 3.5);
  N = beta_binomial_rng(ns, u, alpha);
  N = beta_binomial_rng(ns, u, nu);
  N = beta_binomial_rng(ns, u, ns);
  N = beta_binomial_rng(ns, u, u);
  N = beta_binomial_rng(ns, u, 3);
  N = beta_binomial_rng(ns, u, 3.5);
  N = beta_binomial_rng(ns, 3, alpha);
  N = beta_binomial_rng(ns, 3, nu);
  N = beta_binomial_rng(ns, 3, ns);
  N = beta_binomial_rng(ns, 3, u);
  N = beta_binomial_rng(ns, 3, 3);
  N = beta_binomial_rng(ns, 3, 3.5);
  N = beta_binomial_rng(ns, 3.5, alpha);
  N = beta_binomial_rng(ns, 3.5, nu);
  N = beta_binomial_rng(ns, 3.5, ns);
  N = beta_binomial_rng(ns, 3.5, u);
  N = beta_binomial_rng(ns, 3.5, 3);
  N = beta_binomial_rng(ns, 3.5, 3.5);
  N = beta_binomial_rng(3, alpha, alpha);
  N = beta_binomial_rng(3, alpha, nu);
  N = beta_binomial_rng(3, alpha, ns);
  N = beta_binomial_rng(3, alpha, u);
  N = beta_binomial_rng(3, alpha, 3);
  N = beta_binomial_rng(3, alpha, 3.5);
  N = beta_binomial_rng(3, nu, alpha);
  N = beta_binomial_rng(3, nu, nu);
  N = beta_binomial_rng(3, nu, ns);
  N = beta_binomial_rng(3, nu, u);
  N = beta_binomial_rng(3, nu, 3);
  N = beta_binomial_rng(3, nu, 3.5);
  N = beta_binomial_rng(3, ns, alpha);
  N = beta_binomial_rng(3, ns, nu);
  N = beta_binomial_rng(3, ns, ns);
  N = beta_binomial_rng(3, ns, u);
  N = beta_binomial_rng(3, ns, 3);
  N = beta_binomial_rng(3, ns, 3.5);
  N = beta_binomial_rng(3, u, alpha);
  N = beta_binomial_rng(3, u, nu);
  N = beta_binomial_rng(3, u, ns);
  N = beta_binomial_rng(3, u, u);
  N = beta_binomial_rng(3, u, 3);
  N = beta_binomial_rng(3, u, 3.5);
  N = beta_binomial_rng(3, 3, alpha);
  N = beta_binomial_rng(3, 3, nu);
  N = beta_binomial_rng(3, 3, ns);
  N = beta_binomial_rng(3, 3, u);
  n = beta_binomial_rng(3, 3, 3);
  n = beta_binomial_rng(3, 3, 3.5);
  N = beta_binomial_rng(3, 3.5, alpha);
  N = beta_binomial_rng(3, 3.5, nu);
  N = beta_binomial_rng(3, 3.5, ns);
  N = beta_binomial_rng(3, 3.5, u);
  n = beta_binomial_rng(3, 3.5, 3);
  n = beta_binomial_rng(3, 3.5, 3.5);

  n = hypergeometric_rng(5,4,9);
  n = ordered_logistic_rng(1.9,theta);

  N = bernoulli_rng(alpha);
  N = bernoulli_rng(nu);
  N = bernoulli_rng(ns);
  N = bernoulli_rng(u);
  n = bernoulli_rng(3);
  n = bernoulli_rng(3.5);
  
  N = poisson_rng(alpha);
  N = poisson_rng(nu);
  N = poisson_rng(ns);
  N = poisson_rng(u);
  n = poisson_rng(3);
  n = poisson_rng(3.5);
  
  N = poisson_log_rng(alpha);
  N = poisson_log_rng(nu);
  N = poisson_log_rng(ns);
  N = poisson_log_rng(u);
  n = poisson_log_rng(3);
  n = poisson_log_rng(3.5);
  
  n = categorical_rng(theta);
  n = categorical_logit_rng(theta);
  ns = multinomial_rng(theta,20);

  x = chi_square_rng(alpha);
  x = chi_square_rng(nu);
  x = chi_square_rng(ns);
  x = chi_square_rng(u);
  z = chi_square_rng(3);
  z = chi_square_rng(3.5);
  
  x = inv_chi_square_rng(alpha);
  x = inv_chi_square_rng(nu);
  x = inv_chi_square_rng(ns);
  x = inv_chi_square_rng(u);
  z = inv_chi_square_rng(3);
  z = inv_chi_square_rng(3.5);
  
  x = exponential_rng(alpha);
  x = exponential_rng(nu);
  x = exponential_rng(ns);
  x = exponential_rng(u);
  z = exponential_rng(3);
  z = exponential_rng(3.5);
  
  x = rayleigh_rng(alpha);
  x = rayleigh_rng(nu);
  x = rayleigh_rng(ns);
  x = rayleigh_rng(u);
  z = rayleigh_rng(3);
  z = rayleigh_rng(3.5);

  x = beta_rng(alpha, alpha);
  x = beta_rng(alpha, nu);
  x = beta_rng(alpha, ns);
  x = beta_rng(alpha, u);
  x = beta_rng(alpha, 0.3);
  x = beta_rng(alpha, 0.35);
  x = beta_rng(nu, alpha);
  x = beta_rng(nu, nu);
  x = beta_rng(nu, ns);
  x = beta_rng(nu, u);
  x = beta_rng(nu, 0.3);
  x = beta_rng(nu, 0.35);
  x = beta_rng(ns, alpha);
  x = beta_rng(ns, nu);
  x = beta_rng(ns, ns);
  x = beta_rng(ns, u);
  x = beta_rng(ns, 0.3);
  x = beta_rng(ns, 0.35);
  x = beta_rng(u, alpha);
  x = beta_rng(u, nu);
  x = beta_rng(u, ns);
  x = beta_rng(u, u);
  x = beta_rng(u, 0.3);
  x = beta_rng(u, 0.35);
  x = beta_rng(0.3, alpha);
  x = beta_rng(0.3, nu);
  x = beta_rng(0.3, ns);
  x = beta_rng(0.3, u);
  z = beta_rng(0.3, 0.3);
  z = beta_rng(0.3, 0.35);
  x = beta_rng(0.35, alpha);
  x = beta_rng(0.35, nu);
  x = beta_rng(0.35, ns);
  x = beta_rng(0.35, u);
  z = beta_rng(0.35, 0.3);
  z = beta_rng(0.35, 0.35);

  x = beta_proportion_rng(alpha, alpha);
  x = beta_proportion_rng(alpha, nu);
  x = beta_proportion_rng(alpha, ns);
  x = beta_proportion_rng(alpha, u);
  x = beta_proportion_rng(alpha, 0.3);
  x = beta_proportion_rng(alpha, 0.35);
  x = beta_proportion_rng(nu, alpha);
  x = beta_proportion_rng(nu, nu);
  x = beta_proportion_rng(nu, ns);
  x = beta_proportion_rng(nu, u);
  x = beta_proportion_rng(nu, 0.3);
  x = beta_proportion_rng(nu, 0.35);
  x = beta_proportion_rng(u, alpha);
  x = beta_proportion_rng(u, nu);
  x = beta_proportion_rng(u, ns);
  x = beta_proportion_rng(u, u);
  x = beta_proportion_rng(u, 0.3);
  x = beta_proportion_rng(u, 0.35);
  x = beta_proportion_rng(0.3, alpha);
  x = beta_proportion_rng(0.3, nu);
  x = beta_proportion_rng(0.3, ns);
  x = beta_proportion_rng(0.3, u);
  z = beta_proportion_rng(0.3, 0.3);
  z = beta_proportion_rng(0.3, 0.35);
  x = beta_proportion_rng(0.35, alpha);
  x = beta_proportion_rng(0.35, nu);
  x = beta_proportion_rng(0.35, ns);
  x = beta_proportion_rng(0.35, u);
  z = beta_proportion_rng(0.35, 0.3);
  z = beta_proportion_rng(0.35, 0.35);
  
  x = normal_rng(alpha, alpha);
  x = normal_rng(alpha, nu);
  x = normal_rng(alpha, ns);
  x = normal_rng(alpha, u);
  x = normal_rng(alpha, 3);
  x = normal_rng(alpha, 3.5);
  x = normal_rng(nu, alpha);
  x = normal_rng(nu, nu);
  x = normal_rng(nu, ns);
  x = normal_rng(nu, u);
  x = normal_rng(nu, 3);
  x = normal_rng(nu, 3.5);
  x = normal_rng(ns, alpha);
  x = normal_rng(ns, nu);
  x = normal_rng(ns, ns);
  x = normal_rng(ns, u);
  x = normal_rng(ns, 3);
  x = normal_rng(ns, 3.5);
  x = normal_rng(u, alpha);
  x = normal_rng(u, nu);
  x = normal_rng(u, ns);
  x = normal_rng(u, u);
  x = normal_rng(u, 3);
  x = normal_rng(u, 3.5);
  x = normal_rng(3, alpha);
  x = normal_rng(3, nu);
  x = normal_rng(3, ns);
  x = normal_rng(3, u);
  z = normal_rng(3, 3);
  z = normal_rng(3, 3.5);
  x = normal_rng(3.5, alpha);
  x = normal_rng(3.5, nu);
  x = normal_rng(3.5, ns);
  x = normal_rng(3.5, u);
  z = normal_rng(3.5, 3);
  z = normal_rng(3.5, 3.5);
  
  x = cauchy_rng(alpha, alpha);
  x = cauchy_rng(alpha, nu);
  x = cauchy_rng(alpha, ns);
  x = cauchy_rng(alpha, u);
  x = cauchy_rng(alpha, 3);
  x = cauchy_rng(alpha, 3.5);
  x = cauchy_rng(nu, alpha);
  x = cauchy_rng(nu, nu);
  x = cauchy_rng(nu, ns);
  x = cauchy_rng(nu, u);
  x = cauchy_rng(nu, 3);
  x = cauchy_rng(nu, 3.5);
  x = cauchy_rng(ns, alpha);
  x = cauchy_rng(ns, nu);
  x = cauchy_rng(ns, ns);
  x = cauchy_rng(ns, u);
  x = cauchy_rng(ns, 3);
  x = cauchy_rng(ns, 3.5);
  x = cauchy_rng(u, alpha);
  x = cauchy_rng(u, nu);
  x = cauchy_rng(u, ns);
  x = cauchy_rng(u, u);
  x = cauchy_rng(u, 3);
  x = cauchy_rng(u, 3.5);
  x = cauchy_rng(3, alpha);
  x = cauchy_rng(3, nu);
  x = cauchy_rng(3, ns);
  x = cauchy_rng(3, u);
  z = cauchy_rng(3, 3);
  z = cauchy_rng(3, 3.5);
  x = cauchy_rng(3.5, alpha);
  x = cauchy_rng(3.5, nu);
  x = cauchy_rng(3.5, ns);
  x = cauchy_rng(3.5, u);
  z = cauchy_rng(3.5, 3);
  z = cauchy_rng(3.5, 3.5);
  
  x = double_exponential_rng(alpha, alpha);
  x = double_exponential_rng(alpha, nu);
  x = double_exponential_rng(alpha, ns);
  x = double_exponential_rng(alpha, u);
  x = double_exponential_rng(alpha, 3);
  x = double_exponential_rng(alpha, 3.5);
  x = double_exponential_rng(nu, alpha);
  x = double_exponential_rng(nu, nu);
  x = double_exponential_rng(nu, ns);
  x = double_exponential_rng(nu, u);
  x = double_exponential_rng(nu, 3);
  x = double_exponential_rng(nu, 3.5);
  x = double_exponential_rng(ns, alpha);
  x = double_exponential_rng(ns, nu);
  x = double_exponential_rng(ns, ns);
  x = double_exponential_rng(ns, u);
  x = double_exponential_rng(ns, 3);
  x = double_exponential_rng(ns, 3.5);
  x = double_exponential_rng(u, alpha);
  x = double_exponential_rng(u, nu);
  x = double_exponential_rng(u, ns);
  x = double_exponential_rng(u, u);
  x = double_exponential_rng(u, 3);
  x = double_exponential_rng(u, 3.5);
  x = double_exponential_rng(3, alpha);
  x = double_exponential_rng(3, nu);
  x = double_exponential_rng(3, ns);
  x = double_exponential_rng(3, u);
  z = double_exponential_rng(3, 3);
  z = double_exponential_rng(3, 3.5);
  x = double_exponential_rng(3.5, alpha);
  x = double_exponential_rng(3.5, nu);
  x = double_exponential_rng(3.5, ns);
  x = double_exponential_rng(3.5, u);
  z = double_exponential_rng(3.5, 3);
  z = double_exponential_rng(3.5, 3.5);
  
  x = logistic_rng(alpha, alpha);
  x = logistic_rng(alpha, nu);
  x = logistic_rng(alpha, ns);
  x = logistic_rng(alpha, u);
  x = logistic_rng(alpha, 3);
  x = logistic_rng(alpha, 3.5);
  x = logistic_rng(nu, alpha);
  x = logistic_rng(nu, nu);
  x = logistic_rng(nu, ns);
  x = logistic_rng(nu, u);
  x = logistic_rng(nu, 3);
  x = logistic_rng(nu, 3.5);
  x = logistic_rng(ns, alpha);
  x = logistic_rng(ns, nu);
  x = logistic_rng(ns, ns);
  x = logistic_rng(ns, u);
  x = logistic_rng(ns, 3);
  x = logistic_rng(ns, 3.5);
  x = logistic_rng(u, alpha);
  x = logistic_rng(u, nu);
  x = logistic_rng(u, ns);
  x = logistic_rng(u, u);
  x = logistic_rng(u, 3);
  x = logistic_rng(u, 3.5);
  x = logistic_rng(3, alpha);
  x = logistic_rng(3, nu);
  x = logistic_rng(3, ns);
  x = logistic_rng(3, u);
  z = logistic_rng(3, 3);
  z = logistic_rng(3, 3.5);
  x = logistic_rng(3.5, alpha);
  x = logistic_rng(3.5, nu);
  x = logistic_rng(3.5, ns);
  x = logistic_rng(3.5, u);
  z = logistic_rng(3.5, 3);
  z = logistic_rng(3.5, 3.5);
  
  x = gumbel_rng(alpha, alpha);
  x = gumbel_rng(alpha, nu);
  x = gumbel_rng(alpha, ns);
  x = gumbel_rng(alpha, u);
  x = gumbel_rng(alpha, 3);
  x = gumbel_rng(alpha, 3.5);
  x = gumbel_rng(nu, alpha);
  x = gumbel_rng(nu, nu);
  x = gumbel_rng(nu, ns);
  x = gumbel_rng(nu, u);
  x = gumbel_rng(nu, 3);
  x = gumbel_rng(nu, 3.5);
  x = gumbel_rng(ns, alpha);
  x = gumbel_rng(ns, nu);
  x = gumbel_rng(ns, ns);
  x = gumbel_rng(ns, u);
  x = gumbel_rng(ns, 3);
  x = gumbel_rng(ns, 3.5);
  x = gumbel_rng(u, alpha);
  x = gumbel_rng(u, nu);
  x = gumbel_rng(u, ns);
  x = gumbel_rng(u, u);
  x = gumbel_rng(u, 3);
  x = gumbel_rng(u, 3.5);
  x = gumbel_rng(3, alpha);
  x = gumbel_rng(3, nu);
  x = gumbel_rng(3, ns);
  x = gumbel_rng(3, u);
  z = gumbel_rng(3, 3);
  z = gumbel_rng(3, 3.5);
  x = gumbel_rng(3.5, alpha);
  x = gumbel_rng(3.5, nu);
  x = gumbel_rng(3.5, ns);
  x = gumbel_rng(3.5, u);
  z = gumbel_rng(3.5, 3);
  z = gumbel_rng(3.5, 3.5);
  
  x = lognormal_rng(alpha, alpha);
  x = lognormal_rng(alpha, nu);
  x = lognormal_rng(alpha, ns);
  x = lognormal_rng(alpha, u);
  x = lognormal_rng(alpha, 3);
  x = lognormal_rng(alpha, 3.5);
  x = lognormal_rng(nu, alpha);
  x = lognormal_rng(nu, nu);
  x = lognormal_rng(nu, ns);
  x = lognormal_rng(nu, u);
  x = lognormal_rng(nu, 3);
  x = lognormal_rng(nu, 3.5);
  x = lognormal_rng(ns, alpha);
  x = lognormal_rng(ns, nu);
  x = lognormal_rng(ns, ns);
  x = lognormal_rng(ns, u);
  x = lognormal_rng(ns, 3);
  x = lognormal_rng(ns, 3.5);
  x = lognormal_rng(u, alpha);
  x = lognormal_rng(u, nu);
  x = lognormal_rng(u, ns);
  x = lognormal_rng(u, u);
  x = lognormal_rng(u, 3);
  x = lognormal_rng(u, 3.5);
  x = lognormal_rng(3, alpha);
  x = lognormal_rng(3, nu);
  x = lognormal_rng(3, ns);
  x = lognormal_rng(3, u);
  z = lognormal_rng(3, 3);
  z = lognormal_rng(3, 3.5);
  x = lognormal_rng(3.5, alpha);
  x = lognormal_rng(3.5, nu);
  x = lognormal_rng(3.5, ns);
  x = lognormal_rng(3.5, u);
  z = lognormal_rng(3.5, 3);
  z = lognormal_rng(3.5, 3.5);
  
  x = scaled_inv_chi_square_rng(alpha, alpha);
  x = scaled_inv_chi_square_rng(alpha, nu);
  x = scaled_inv_chi_square_rng(alpha, ns);
  x = scaled_inv_chi_square_rng(alpha, u);
  x = scaled_inv_chi_square_rng(alpha, 3);
  x = scaled_inv_chi_square_rng(alpha, 3.5);
  x = scaled_inv_chi_square_rng(nu, alpha);
  x = scaled_inv_chi_square_rng(nu, nu);
  x = scaled_inv_chi_square_rng(nu, ns);
  x = scaled_inv_chi_square_rng(nu, u);
  x = scaled_inv_chi_square_rng(nu, 3);
  x = scaled_inv_chi_square_rng(nu, 3.5);
  x = scaled_inv_chi_square_rng(ns, alpha);
  x = scaled_inv_chi_square_rng(ns, nu);
  x = scaled_inv_chi_square_rng(ns, ns);
  x = scaled_inv_chi_square_rng(ns, u);
  x = scaled_inv_chi_square_rng(ns, 3);
  x = scaled_inv_chi_square_rng(ns, 3.5);
  x = scaled_inv_chi_square_rng(u, alpha);
  x = scaled_inv_chi_square_rng(u, nu);
  x = scaled_inv_chi_square_rng(u, ns);
  x = scaled_inv_chi_square_rng(u, u);
  x = scaled_inv_chi_square_rng(u, 3);
  x = scaled_inv_chi_square_rng(u, 3.5);
  x = scaled_inv_chi_square_rng(3, alpha);
  x = scaled_inv_chi_square_rng(3, nu);
  x = scaled_inv_chi_square_rng(3, ns);
  x = scaled_inv_chi_square_rng(3, u);
  z = scaled_inv_chi_square_rng(3, 3);
  z = scaled_inv_chi_square_rng(3, 3.5);
  x = scaled_inv_chi_square_rng(3.5, alpha);
  x = scaled_inv_chi_square_rng(3.5, nu);
  x = scaled_inv_chi_square_rng(3.5, ns);
  x = scaled_inv_chi_square_rng(3.5, u);
  z = scaled_inv_chi_square_rng(3.5, 3);
  z = scaled_inv_chi_square_rng(3.5, 3.5);
  
  x = gamma_rng(alpha, alpha);
  x = gamma_rng(alpha, nu);
  x = gamma_rng(alpha, ns);
  x = gamma_rng(alpha, u);
  x = gamma_rng(alpha, 3);
  x = gamma_rng(alpha, 3.5);
  x = gamma_rng(nu, alpha);
  x = gamma_rng(nu, nu);
  x = gamma_rng(nu, ns);
  x = gamma_rng(nu, u);
  x = gamma_rng(nu, 3);
  x = gamma_rng(nu, 3.5);
  x = gamma_rng(ns, alpha);
  x = gamma_rng(ns, nu);
  x = gamma_rng(ns, ns);
  x = gamma_rng(ns, u);
  x = gamma_rng(ns, 3);
  x = gamma_rng(ns, 3.5);
  x = gamma_rng(u, alpha);
  x = gamma_rng(u, nu);
  x = gamma_rng(u, ns);
  x = gamma_rng(u, u);
  x = gamma_rng(u, 3);
  x = gamma_rng(u, 3.5);
  x = gamma_rng(3, alpha);
  x = gamma_rng(3, nu);
  x = gamma_rng(3, ns);
  x = gamma_rng(3, u);
  z = gamma_rng(3, 3);
  z = gamma_rng(3, 3.5);
  x = gamma_rng(3.5, alpha);
  x = gamma_rng(3.5, nu);
  x = gamma_rng(3.5, ns);
  x = gamma_rng(3.5, u);
  z = gamma_rng(3.5, 3);
  z = gamma_rng(3.5, 3.5);
  
  x = inv_gamma_rng(alpha, alpha);
  x = inv_gamma_rng(alpha, nu);
  x = inv_gamma_rng(alpha, ns);
  x = inv_gamma_rng(alpha, u);
  x = inv_gamma_rng(alpha, 3);
  x = inv_gamma_rng(alpha, 3.5);
  x = inv_gamma_rng(nu, alpha);
  x = inv_gamma_rng(nu, nu);
  x = inv_gamma_rng(nu, ns);
  x = inv_gamma_rng(nu, u);
  x = inv_gamma_rng(nu, 3);
  x = inv_gamma_rng(nu, 3.5);
  x = inv_gamma_rng(ns, alpha);
  x = inv_gamma_rng(ns, nu);
  x = inv_gamma_rng(ns, ns);
  x = inv_gamma_rng(ns, u);
  x = inv_gamma_rng(ns, 3);
  x = inv_gamma_rng(ns, 3.5);
  x = inv_gamma_rng(u, alpha);
  x = inv_gamma_rng(u, nu);
  x = inv_gamma_rng(u, ns);
  x = inv_gamma_rng(u, u);
  x = inv_gamma_rng(u, 3);
  x = inv_gamma_rng(u, 3.5);
  x = inv_gamma_rng(3, alpha);
  x = inv_gamma_rng(3, nu);
  x = inv_gamma_rng(3, ns);
  x = inv_gamma_rng(3, u);
  z = inv_gamma_rng(3, 3);
  z = inv_gamma_rng(3, 3.5);
  x = inv_gamma_rng(3.5, alpha);
  x = inv_gamma_rng(3.5, nu);
  x = inv_gamma_rng(3.5, ns);
  x = inv_gamma_rng(3.5, u);
  z = inv_gamma_rng(3.5, 3);
  z = inv_gamma_rng(3.5, 3.5);
  
  x = weibull_rng(alpha, alpha);
  x = weibull_rng(alpha, nu);
  x = weibull_rng(alpha, ns);
  x = weibull_rng(alpha, u);
  x = weibull_rng(alpha, 3);
  x = weibull_rng(alpha, 3.5);
  x = weibull_rng(nu, alpha);
  x = weibull_rng(nu, nu);
  x = weibull_rng(nu, ns);
  x = weibull_rng(nu, u);
  x = weibull_rng(nu, 3);
  x = weibull_rng(nu, 3.5);
  x = weibull_rng(ns, alpha);
  x = weibull_rng(ns, nu);
  x = weibull_rng(ns, ns);
  x = weibull_rng(ns, u);
  x = weibull_rng(ns, 3);
  x = weibull_rng(ns, 3.5);
  x = weibull_rng(u, alpha);
  x = weibull_rng(u, nu);
  x = weibull_rng(u, ns);
  x = weibull_rng(u, u);
  x = weibull_rng(u, 3);
  x = weibull_rng(u, 3.5);
  x = weibull_rng(3, alpha);
  x = weibull_rng(3, nu);
  x = weibull_rng(3, ns);
  x = weibull_rng(3, u);
  z = weibull_rng(3, 3);
  z = weibull_rng(3, 3.5);
  x = weibull_rng(3.5, alpha);
  x = weibull_rng(3.5, nu);
  x = weibull_rng(3.5, ns);
  x = weibull_rng(3.5, u);
  z = weibull_rng(3.5, 3);
  z = weibull_rng(3.5, 3.5);
  
  x = frechet_rng(alpha, alpha);
  x = frechet_rng(alpha, nu);
  x = frechet_rng(alpha, ns);
  x = frechet_rng(alpha, u);
  x = frechet_rng(alpha, 3);
  x = frechet_rng(alpha, 3.5);
  x = frechet_rng(nu, alpha);
  x = frechet_rng(nu, nu);
  x = frechet_rng(nu, ns);
  x = frechet_rng(nu, u);
  x = frechet_rng(nu, 3);
  x = frechet_rng(nu, 3.5);
  x = frechet_rng(ns, alpha);
  x = frechet_rng(ns, nu);
  x = frechet_rng(ns, ns);
  x = frechet_rng(ns, u);
  x = frechet_rng(ns, 3);
  x = frechet_rng(ns, 3.5);
  x = frechet_rng(u, alpha);
  x = frechet_rng(u, nu);
  x = frechet_rng(u, ns);
  x = frechet_rng(u, u);
  x = frechet_rng(u, 3);
  x = frechet_rng(u, 3.5);
  x = frechet_rng(3, alpha);
  x = frechet_rng(3, nu);
  x = frechet_rng(3, ns);
  x = frechet_rng(3, u);
  z = frechet_rng(3, 3);
  z = frechet_rng(3, 3.5);
  x = frechet_rng(3.5, alpha);
  x = frechet_rng(3.5, nu);
  x = frechet_rng(3.5, ns);
  x = frechet_rng(3.5, u);
  z = frechet_rng(3.5, 3);
  z = frechet_rng(3.5, 3.5);
  
  x = pareto_rng(alpha, alpha);
  x = pareto_rng(alpha, nu);
  x = pareto_rng(alpha, ns);
  x = pareto_rng(alpha, u);
  x = pareto_rng(alpha, 3);
  x = pareto_rng(alpha, 3.5);
  x = pareto_rng(nu, alpha);
  x = pareto_rng(nu, nu);
  x = pareto_rng(nu, ns);
  x = pareto_rng(nu, u);
  x = pareto_rng(nu, 3);
  x = pareto_rng(nu, 3.5);
  x = pareto_rng(ns, alpha);
  x = pareto_rng(ns, nu);
  x = pareto_rng(ns, ns);
  x = pareto_rng(ns, u);
  x = pareto_rng(ns, 3);
  x = pareto_rng(ns, 3.5);
  x = pareto_rng(u, alpha);
  x = pareto_rng(u, nu);
  x = pareto_rng(u, ns);
  x = pareto_rng(u, u);
  x = pareto_rng(u, 3);
  x = pareto_rng(u, 3.5);
  x = pareto_rng(3, alpha);
  x = pareto_rng(3, nu);
  x = pareto_rng(3, ns);
  x = pareto_rng(3, u);
  z = pareto_rng(3, 3);
  z = pareto_rng(3, 3.5);
  x = pareto_rng(3.5, alpha);
  x = pareto_rng(3.5, nu);
  x = pareto_rng(3.5, ns);
  x = pareto_rng(3.5, u);
  z = pareto_rng(3.5, 3);
  z = pareto_rng(3.5, 3.5);

  x = exp_mod_normal_rng(alpha, alpha, alpha);
  x = exp_mod_normal_rng(alpha, alpha, nu);
  x = exp_mod_normal_rng(alpha, alpha, ns);
  x = exp_mod_normal_rng(alpha, alpha, u);
  x = exp_mod_normal_rng(alpha, alpha, 3);
  x = exp_mod_normal_rng(alpha, alpha, 3.5);
  x = exp_mod_normal_rng(alpha, nu, alpha);
  x = exp_mod_normal_rng(alpha, nu, nu);
  x = exp_mod_normal_rng(alpha, nu, ns);
  x = exp_mod_normal_rng(alpha, nu, u);
  x = exp_mod_normal_rng(alpha, nu, 3);
  x = exp_mod_normal_rng(alpha, nu, 3.5);
  x = exp_mod_normal_rng(alpha, ns, alpha);
  x = exp_mod_normal_rng(alpha, ns, nu);
  x = exp_mod_normal_rng(alpha, ns, ns);
  x = exp_mod_normal_rng(alpha, ns, u);
  x = exp_mod_normal_rng(alpha, ns, 3);
  x = exp_mod_normal_rng(alpha, ns, 3.5);
  x = exp_mod_normal_rng(alpha, u, alpha);
  x = exp_mod_normal_rng(alpha, u, nu);
  x = exp_mod_normal_rng(alpha, u, ns);
  x = exp_mod_normal_rng(alpha, u, u);
  x = exp_mod_normal_rng(alpha, u, 3);
  x = exp_mod_normal_rng(alpha, u, 3.5);
  x = exp_mod_normal_rng(alpha, 3, alpha);
  x = exp_mod_normal_rng(alpha, 3, nu);
  x = exp_mod_normal_rng(alpha, 3, ns);
  x = exp_mod_normal_rng(alpha, 3, u);
  x = exp_mod_normal_rng(alpha, 3, 3);
  x = exp_mod_normal_rng(alpha, 3, 3.5);
  x = exp_mod_normal_rng(alpha, 3.5, alpha);
  x = exp_mod_normal_rng(alpha, 3.5, nu);
  x = exp_mod_normal_rng(alpha, 3.5, ns);
  x = exp_mod_normal_rng(alpha, 3.5, u);
  x = exp_mod_normal_rng(alpha, 3.5, 3);
  x = exp_mod_normal_rng(alpha, 3.5, 3.5);
  x = exp_mod_normal_rng(nu, alpha, alpha);
  x = exp_mod_normal_rng(nu, alpha, nu);
  x = exp_mod_normal_rng(nu, alpha, ns);
  x = exp_mod_normal_rng(nu, alpha, u);
  x = exp_mod_normal_rng(nu, alpha, 3);
  x = exp_mod_normal_rng(nu, alpha, 3.5);
  x = exp_mod_normal_rng(nu, nu, alpha);
  x = exp_mod_normal_rng(nu, nu, nu);
  x = exp_mod_normal_rng(nu, nu, ns);
  x = exp_mod_normal_rng(nu, nu, u);
  x = exp_mod_normal_rng(nu, nu, 3);
  x = exp_mod_normal_rng(nu, nu, 3.5);
  x = exp_mod_normal_rng(nu, ns, alpha);
  x = exp_mod_normal_rng(nu, ns, nu);
  x = exp_mod_normal_rng(nu, ns, ns);
  x = exp_mod_normal_rng(nu, ns, u);
  x = exp_mod_normal_rng(nu, ns, 3);
  x = exp_mod_normal_rng(nu, ns, 3.5);
  x = exp_mod_normal_rng(nu, u, alpha);
  x = exp_mod_normal_rng(nu, u, nu);
  x = exp_mod_normal_rng(nu, u, ns);
  x = exp_mod_normal_rng(nu, u, u);
  x = exp_mod_normal_rng(nu, u, 3);
  x = exp_mod_normal_rng(nu, u, 3.5);
  x = exp_mod_normal_rng(nu, 3, alpha);
  x = exp_mod_normal_rng(nu, 3, nu);
  x = exp_mod_normal_rng(nu, 3, ns);
  x = exp_mod_normal_rng(nu, 3, u);
  x = exp_mod_normal_rng(nu, 3, 3);
  x = exp_mod_normal_rng(nu, 3, 3.5);
  x = exp_mod_normal_rng(nu, 3.5, alpha);
  x = exp_mod_normal_rng(nu, 3.5, nu);
  x = exp_mod_normal_rng(nu, 3.5, ns);
  x = exp_mod_normal_rng(nu, 3.5, u);
  x = exp_mod_normal_rng(nu, 3.5, 3);
  x = exp_mod_normal_rng(nu, 3.5, 3.5);
  x = exp_mod_normal_rng(ns, alpha, alpha);
  x = exp_mod_normal_rng(ns, alpha, nu);
  x = exp_mod_normal_rng(ns, alpha, ns);
  x = exp_mod_normal_rng(ns, alpha, u);
  x = exp_mod_normal_rng(ns, alpha, 3);
  x = exp_mod_normal_rng(ns, alpha, 3.5);
  x = exp_mod_normal_rng(ns, nu, alpha);
  x = exp_mod_normal_rng(ns, nu, nu);
  x = exp_mod_normal_rng(ns, nu, ns);
  x = exp_mod_normal_rng(ns, nu, u);
  x = exp_mod_normal_rng(ns, nu, 3);
  x = exp_mod_normal_rng(ns, nu, 3.5);
  x = exp_mod_normal_rng(ns, ns, alpha);
  x = exp_mod_normal_rng(ns, ns, nu);
  x = exp_mod_normal_rng(ns, ns, ns);
  x = exp_mod_normal_rng(ns, ns, u);
  x = exp_mod_normal_rng(ns, ns, 3);
  x = exp_mod_normal_rng(ns, ns, 3.5);
  x = exp_mod_normal_rng(ns, u, alpha);
  x = exp_mod_normal_rng(ns, u, nu);
  x = exp_mod_normal_rng(ns, u, ns);
  x = exp_mod_normal_rng(ns, u, u);
  x = exp_mod_normal_rng(ns, u, 3);
  x = exp_mod_normal_rng(ns, u, 3.5);
  x = exp_mod_normal_rng(ns, 3, alpha);
  x = exp_mod_normal_rng(ns, 3, nu);
  x = exp_mod_normal_rng(ns, 3, ns);
  x = exp_mod_normal_rng(ns, 3, u);
  x = exp_mod_normal_rng(ns, 3, 3);
  x = exp_mod_normal_rng(ns, 3, 3.5);
  x = exp_mod_normal_rng(ns, 3.5, alpha);
  x = exp_mod_normal_rng(ns, 3.5, nu);
  x = exp_mod_normal_rng(ns, 3.5, ns);
  x = exp_mod_normal_rng(ns, 3.5, u);
  x = exp_mod_normal_rng(ns, 3.5, 3);
  x = exp_mod_normal_rng(ns, 3.5, 3.5);
  x = exp_mod_normal_rng(u, alpha, alpha);
  x = exp_mod_normal_rng(u, alpha, nu);
  x = exp_mod_normal_rng(u, alpha, ns);
  x = exp_mod_normal_rng(u, alpha, u);
  x = exp_mod_normal_rng(u, alpha, 3);
  x = exp_mod_normal_rng(u, alpha, 3.5);
  x = exp_mod_normal_rng(u, nu, alpha);
  x = exp_mod_normal_rng(u, nu, nu);
  x = exp_mod_normal_rng(u, nu, ns);
  x = exp_mod_normal_rng(u, nu, u);
  x = exp_mod_normal_rng(u, nu, 3);
  x = exp_mod_normal_rng(u, nu, 3.5);
  x = exp_mod_normal_rng(u, ns, alpha);
  x = exp_mod_normal_rng(u, ns, nu);
  x = exp_mod_normal_rng(u, ns, ns);
  x = exp_mod_normal_rng(u, ns, u);
  x = exp_mod_normal_rng(u, ns, 3);
  x = exp_mod_normal_rng(u, ns, 3.5);
  x = exp_mod_normal_rng(u, u, alpha);
  x = exp_mod_normal_rng(u, u, nu);
  x = exp_mod_normal_rng(u, u, ns);
  x = exp_mod_normal_rng(u, u, u);
  x = exp_mod_normal_rng(u, u, 3);
  x = exp_mod_normal_rng(u, u, 3.5);
  x = exp_mod_normal_rng(u, 3, alpha);
  x = exp_mod_normal_rng(u, 3, nu);
  x = exp_mod_normal_rng(u, 3, ns);
  x = exp_mod_normal_rng(u, 3, u);
  x = exp_mod_normal_rng(u, 3, 3);
  x = exp_mod_normal_rng(u, 3, 3.5);
  x = exp_mod_normal_rng(u, 3.5, alpha);
  x = exp_mod_normal_rng(u, 3.5, nu);
  x = exp_mod_normal_rng(u, 3.5, ns);
  x = exp_mod_normal_rng(u, 3.5, u);
  x = exp_mod_normal_rng(u, 3.5, 3);
  x = exp_mod_normal_rng(u, 3.5, 3.5);
  x = exp_mod_normal_rng(3, alpha, alpha);
  x = exp_mod_normal_rng(3, alpha, nu);
  x = exp_mod_normal_rng(3, alpha, ns);
  x = exp_mod_normal_rng(3, alpha, u);
  x = exp_mod_normal_rng(3, alpha, 3);
  x = exp_mod_normal_rng(3, alpha, 3.5);
  x = exp_mod_normal_rng(3, nu, alpha);
  x = exp_mod_normal_rng(3, nu, nu);
  x = exp_mod_normal_rng(3, nu, ns);
  x = exp_mod_normal_rng(3, nu, u);
  x = exp_mod_normal_rng(3, nu, 3);
  x = exp_mod_normal_rng(3, nu, 3.5);
  x = exp_mod_normal_rng(3, ns, alpha);
  x = exp_mod_normal_rng(3, ns, nu);
  x = exp_mod_normal_rng(3, ns, ns);
  x = exp_mod_normal_rng(3, ns, u);
  x = exp_mod_normal_rng(3, ns, 3);
  x = exp_mod_normal_rng(3, ns, 3.5);
  x = exp_mod_normal_rng(3, u, alpha);
  x = exp_mod_normal_rng(3, u, nu);
  x = exp_mod_normal_rng(3, u, ns);
  x = exp_mod_normal_rng(3, u, u);
  x = exp_mod_normal_rng(3, u, 3);
  x = exp_mod_normal_rng(3, u, 3.5);
  x = exp_mod_normal_rng(3, 3, alpha);
  x = exp_mod_normal_rng(3, 3, nu);
  x = exp_mod_normal_rng(3, 3, ns);
  x = exp_mod_normal_rng(3, 3, u);
  z = exp_mod_normal_rng(3, 3, 3);
  z = exp_mod_normal_rng(3, 3, 3.5);
  x = exp_mod_normal_rng(3, 3.5, alpha);
  x = exp_mod_normal_rng(3, 3.5, nu);
  x = exp_mod_normal_rng(3, 3.5, ns);
  x = exp_mod_normal_rng(3, 3.5, u);
  z = exp_mod_normal_rng(3, 3.5, 3);
  z = exp_mod_normal_rng(3, 3.5, 3.5);
  x = exp_mod_normal_rng(3.5, alpha, alpha);
  x = exp_mod_normal_rng(3.5, alpha, nu);
  x = exp_mod_normal_rng(3.5, alpha, ns);
  x = exp_mod_normal_rng(3.5, alpha, u);
  x = exp_mod_normal_rng(3.5, alpha, 3);
  x = exp_mod_normal_rng(3.5, alpha, 3.5);
  x = exp_mod_normal_rng(3.5, nu, alpha);
  x = exp_mod_normal_rng(3.5, nu, nu);
  x = exp_mod_normal_rng(3.5, nu, ns);
  x = exp_mod_normal_rng(3.5, nu, u);
  x = exp_mod_normal_rng(3.5, nu, 3);
  x = exp_mod_normal_rng(3.5, nu, 3.5);
  x = exp_mod_normal_rng(3.5, ns, alpha);
  x = exp_mod_normal_rng(3.5, ns, nu);
  x = exp_mod_normal_rng(3.5, ns, ns);
  x = exp_mod_normal_rng(3.5, ns, u);
  x = exp_mod_normal_rng(3.5, ns, 3);
  x = exp_mod_normal_rng(3.5, ns, 3.5);
  x = exp_mod_normal_rng(3.5, u, alpha);
  x = exp_mod_normal_rng(3.5, u, nu);
  x = exp_mod_normal_rng(3.5, u, ns);
  x = exp_mod_normal_rng(3.5, u, u);
  x = exp_mod_normal_rng(3.5, u, 3);
  x = exp_mod_normal_rng(3.5, u, 3.5);
  x = exp_mod_normal_rng(3.5, 3, alpha);
  x = exp_mod_normal_rng(3.5, 3, nu);
  x = exp_mod_normal_rng(3.5, 3, ns);
  x = exp_mod_normal_rng(3.5, 3, u);
  z = exp_mod_normal_rng(3.5, 3, 3);
  z = exp_mod_normal_rng(3.5, 3, 3.5);
  x = exp_mod_normal_rng(3.5, 3.5, alpha);
  x = exp_mod_normal_rng(3.5, 3.5, nu);
  x = exp_mod_normal_rng(3.5, 3.5, ns);
  x = exp_mod_normal_rng(3.5, 3.5, u);
  z = exp_mod_normal_rng(3.5, 3.5, 3);
  z = exp_mod_normal_rng(3.5, 3.5, 3.5);
  
  x = pareto_type_2_rng(alpha, alpha, alpha);
  x = pareto_type_2_rng(alpha, alpha, nu);
  x = pareto_type_2_rng(alpha, alpha, ns);
  x = pareto_type_2_rng(alpha, alpha, u);
  x = pareto_type_2_rng(alpha, alpha, 3);
  x = pareto_type_2_rng(alpha, alpha, 3.5);
  x = pareto_type_2_rng(alpha, nu, alpha);
  x = pareto_type_2_rng(alpha, nu, nu);
  x = pareto_type_2_rng(alpha, nu, ns);
  x = pareto_type_2_rng(alpha, nu, u);
  x = pareto_type_2_rng(alpha, nu, 3);
  x = pareto_type_2_rng(alpha, nu, 3.5);
  x = pareto_type_2_rng(alpha, ns, alpha);
  x = pareto_type_2_rng(alpha, ns, nu);
  x = pareto_type_2_rng(alpha, ns, ns);
  x = pareto_type_2_rng(alpha, ns, u);
  x = pareto_type_2_rng(alpha, ns, 3);
  x = pareto_type_2_rng(alpha, ns, 3.5);
  x = pareto_type_2_rng(alpha, u, alpha);
  x = pareto_type_2_rng(alpha, u, nu);
  x = pareto_type_2_rng(alpha, u, ns);
  x = pareto_type_2_rng(alpha, u, u);
  x = pareto_type_2_rng(alpha, u, 3);
  x = pareto_type_2_rng(alpha, u, 3.5);
  x = pareto_type_2_rng(alpha, 3, alpha);
  x = pareto_type_2_rng(alpha, 3, nu);
  x = pareto_type_2_rng(alpha, 3, ns);
  x = pareto_type_2_rng(alpha, 3, u);
  x = pareto_type_2_rng(alpha, 3, 3);
  x = pareto_type_2_rng(alpha, 3, 3.5);
  x = pareto_type_2_rng(alpha, 3.5, alpha);
  x = pareto_type_2_rng(alpha, 3.5, nu);
  x = pareto_type_2_rng(alpha, 3.5, ns);
  x = pareto_type_2_rng(alpha, 3.5, u);
  x = pareto_type_2_rng(alpha, 3.5, 3);
  x = pareto_type_2_rng(alpha, 3.5, 3.5);
  x = pareto_type_2_rng(nu, alpha, alpha);
  x = pareto_type_2_rng(nu, alpha, nu);
  x = pareto_type_2_rng(nu, alpha, ns);
  x = pareto_type_2_rng(nu, alpha, u);
  x = pareto_type_2_rng(nu, alpha, 3);
  x = pareto_type_2_rng(nu, alpha, 3.5);
  x = pareto_type_2_rng(nu, nu, alpha);
  x = pareto_type_2_rng(nu, nu, nu);
  x = pareto_type_2_rng(nu, nu, ns);
  x = pareto_type_2_rng(nu, nu, u);
  x = pareto_type_2_rng(nu, nu, 3);
  x = pareto_type_2_rng(nu, nu, 3.5);
  x = pareto_type_2_rng(nu, ns, alpha);
  x = pareto_type_2_rng(nu, ns, nu);
  x = pareto_type_2_rng(nu, ns, ns);
  x = pareto_type_2_rng(nu, ns, u);
  x = pareto_type_2_rng(nu, ns, 3);
  x = pareto_type_2_rng(nu, ns, 3.5);
  x = pareto_type_2_rng(nu, u, alpha);
  x = pareto_type_2_rng(nu, u, nu);
  x = pareto_type_2_rng(nu, u, ns);
  x = pareto_type_2_rng(nu, u, u);
  x = pareto_type_2_rng(nu, u, 3);
  x = pareto_type_2_rng(nu, u, 3.5);
  x = pareto_type_2_rng(nu, 3, alpha);
  x = pareto_type_2_rng(nu, 3, nu);
  x = pareto_type_2_rng(nu, 3, ns);
  x = pareto_type_2_rng(nu, 3, u);
  x = pareto_type_2_rng(nu, 3, 3);
  x = pareto_type_2_rng(nu, 3, 3.5);
  x = pareto_type_2_rng(nu, 3.5, alpha);
  x = pareto_type_2_rng(nu, 3.5, nu);
  x = pareto_type_2_rng(nu, 3.5, ns);
  x = pareto_type_2_rng(nu, 3.5, u);
  x = pareto_type_2_rng(nu, 3.5, 3);
  x = pareto_type_2_rng(nu, 3.5, 3.5);
  x = pareto_type_2_rng(ns, alpha, alpha);
  x = pareto_type_2_rng(ns, alpha, nu);
  x = pareto_type_2_rng(ns, alpha, ns);
  x = pareto_type_2_rng(ns, alpha, u);
  x = pareto_type_2_rng(ns, alpha, 3);
  x = pareto_type_2_rng(ns, alpha, 3.5);
  x = pareto_type_2_rng(ns, nu, alpha);
  x = pareto_type_2_rng(ns, nu, nu);
  x = pareto_type_2_rng(ns, nu, ns);
  x = pareto_type_2_rng(ns, nu, u);
  x = pareto_type_2_rng(ns, nu, 3);
  x = pareto_type_2_rng(ns, nu, 3.5);
  x = pareto_type_2_rng(ns, ns, alpha);
  x = pareto_type_2_rng(ns, ns, nu);
  x = pareto_type_2_rng(ns, ns, ns);
  x = pareto_type_2_rng(ns, ns, u);
  x = pareto_type_2_rng(ns, ns, 3);
  x = pareto_type_2_rng(ns, ns, 3.5);
  x = pareto_type_2_rng(ns, u, alpha);
  x = pareto_type_2_rng(ns, u, nu);
  x = pareto_type_2_rng(ns, u, ns);
  x = pareto_type_2_rng(ns, u, u);
  x = pareto_type_2_rng(ns, u, 3);
  x = pareto_type_2_rng(ns, u, 3.5);
  x = pareto_type_2_rng(ns, 3, alpha);
  x = pareto_type_2_rng(ns, 3, nu);
  x = pareto_type_2_rng(ns, 3, ns);
  x = pareto_type_2_rng(ns, 3, u);
  x = pareto_type_2_rng(ns, 3, 3);
  x = pareto_type_2_rng(ns, 3, 3.5);
  x = pareto_type_2_rng(ns, 3.5, alpha);
  x = pareto_type_2_rng(ns, 3.5, nu);
  x = pareto_type_2_rng(ns, 3.5, ns);
  x = pareto_type_2_rng(ns, 3.5, u);
  x = pareto_type_2_rng(ns, 3.5, 3);
  x = pareto_type_2_rng(ns, 3.5, 3.5);
  x = pareto_type_2_rng(u, alpha, alpha);
  x = pareto_type_2_rng(u, alpha, nu);
  x = pareto_type_2_rng(u, alpha, ns);
  x = pareto_type_2_rng(u, alpha, u);
  x = pareto_type_2_rng(u, alpha, 3);
  x = pareto_type_2_rng(u, alpha, 3.5);
  x = pareto_type_2_rng(u, nu, alpha);
  x = pareto_type_2_rng(u, nu, nu);
  x = pareto_type_2_rng(u, nu, ns);
  x = pareto_type_2_rng(u, nu, u);
  x = pareto_type_2_rng(u, nu, 3);
  x = pareto_type_2_rng(u, nu, 3.5);
  x = pareto_type_2_rng(u, ns, alpha);
  x = pareto_type_2_rng(u, ns, nu);
  x = pareto_type_2_rng(u, ns, ns);
  x = pareto_type_2_rng(u, ns, u);
  x = pareto_type_2_rng(u, ns, 3);
  x = pareto_type_2_rng(u, ns, 3.5);
  x = pareto_type_2_rng(u, u, alpha);
  x = pareto_type_2_rng(u, u, nu);
  x = pareto_type_2_rng(u, u, ns);
  x = pareto_type_2_rng(u, u, u);
  x = pareto_type_2_rng(u, u, 3);
  x = pareto_type_2_rng(u, u, 3.5);
  x = pareto_type_2_rng(u, 3, alpha);
  x = pareto_type_2_rng(u, 3, nu);
  x = pareto_type_2_rng(u, 3, ns);
  x = pareto_type_2_rng(u, 3, u);
  x = pareto_type_2_rng(u, 3, 3);
  x = pareto_type_2_rng(u, 3, 3.5);
  x = pareto_type_2_rng(u, 3.5, alpha);
  x = pareto_type_2_rng(u, 3.5, nu);
  x = pareto_type_2_rng(u, 3.5, ns);
  x = pareto_type_2_rng(u, 3.5, u);
  x = pareto_type_2_rng(u, 3.5, 3);
  x = pareto_type_2_rng(u, 3.5, 3.5);
  x = pareto_type_2_rng(3, alpha, alpha);
  x = pareto_type_2_rng(3, alpha, nu);
  x = pareto_type_2_rng(3, alpha, ns);
  x = pareto_type_2_rng(3, alpha, u);
  x = pareto_type_2_rng(3, alpha, 3);
  x = pareto_type_2_rng(3, alpha, 3.5);
  x = pareto_type_2_rng(3, nu, alpha);
  x = pareto_type_2_rng(3, nu, nu);
  x = pareto_type_2_rng(3, nu, ns);
  x = pareto_type_2_rng(3, nu, u);
  x = pareto_type_2_rng(3, nu, 3);
  x = pareto_type_2_rng(3, nu, 3.5);
  x = pareto_type_2_rng(3, ns, alpha);
  x = pareto_type_2_rng(3, ns, nu);
  x = pareto_type_2_rng(3, ns, ns);
  x = pareto_type_2_rng(3, ns, u);
  x = pareto_type_2_rng(3, ns, 3);
  x = pareto_type_2_rng(3, ns, 3.5);
  x = pareto_type_2_rng(3, u, alpha);
  x = pareto_type_2_rng(3, u, nu);
  x = pareto_type_2_rng(3, u, ns);
  x = pareto_type_2_rng(3, u, u);
  x = pareto_type_2_rng(3, u, 3);
  x = pareto_type_2_rng(3, u, 3.5);
  x = pareto_type_2_rng(3, 3, alpha);
  x = pareto_type_2_rng(3, 3, nu);
  x = pareto_type_2_rng(3, 3, ns);
  x = pareto_type_2_rng(3, 3, u);
  z = pareto_type_2_rng(3, 3, 3);
  z = pareto_type_2_rng(3, 3, 3.5);
  x = pareto_type_2_rng(3, 3.5, alpha);
  x = pareto_type_2_rng(3, 3.5, nu);
  x = pareto_type_2_rng(3, 3.5, ns);
  x = pareto_type_2_rng(3, 3.5, u);
  z = pareto_type_2_rng(3, 3.5, 3);
  z = pareto_type_2_rng(3, 3.5, 3.5);
  x = pareto_type_2_rng(3.5, alpha, alpha);
  x = pareto_type_2_rng(3.5, alpha, nu);
  x = pareto_type_2_rng(3.5, alpha, ns);
  x = pareto_type_2_rng(3.5, alpha, u);
  x = pareto_type_2_rng(3.5, alpha, 3);
  x = pareto_type_2_rng(3.5, alpha, 3.5);
  x = pareto_type_2_rng(3.5, nu, alpha);
  x = pareto_type_2_rng(3.5, nu, nu);
  x = pareto_type_2_rng(3.5, nu, ns);
  x = pareto_type_2_rng(3.5, nu, u);
  x = pareto_type_2_rng(3.5, nu, 3);
  x = pareto_type_2_rng(3.5, nu, 3.5);
  x = pareto_type_2_rng(3.5, ns, alpha);
  x = pareto_type_2_rng(3.5, ns, nu);
  x = pareto_type_2_rng(3.5, ns, ns);
  x = pareto_type_2_rng(3.5, ns, u);
  x = pareto_type_2_rng(3.5, ns, 3);
  x = pareto_type_2_rng(3.5, ns, 3.5);
  x = pareto_type_2_rng(3.5, u, alpha);
  x = pareto_type_2_rng(3.5, u, nu);
  x = pareto_type_2_rng(3.5, u, ns);
  x = pareto_type_2_rng(3.5, u, u);
  x = pareto_type_2_rng(3.5, u, 3);
  x = pareto_type_2_rng(3.5, u, 3.5);
  x = pareto_type_2_rng(3.5, 3, alpha);
  x = pareto_type_2_rng(3.5, 3, nu);
  x = pareto_type_2_rng(3.5, 3, ns);
  x = pareto_type_2_rng(3.5, 3, u);
  z = pareto_type_2_rng(3.5, 3, 3);
  z = pareto_type_2_rng(3.5, 3, 3.5);
  x = pareto_type_2_rng(3.5, 3.5, alpha);
  x = pareto_type_2_rng(3.5, 3.5, nu);
  x = pareto_type_2_rng(3.5, 3.5, ns);
  x = pareto_type_2_rng(3.5, 3.5, u);
  z = pareto_type_2_rng(3.5, 3.5, 3);
  z = pareto_type_2_rng(3.5, 3.5, 3.5);
  
  x = skew_normal_rng(alpha, alpha, alpha);
  x = skew_normal_rng(alpha, alpha, nu);
  x = skew_normal_rng(alpha, alpha, ns);
  x = skew_normal_rng(alpha, alpha, u);
  x = skew_normal_rng(alpha, alpha, 3);
  x = skew_normal_rng(alpha, alpha, 3.5);
  x = skew_normal_rng(alpha, nu, alpha);
  x = skew_normal_rng(alpha, nu, nu);
  x = skew_normal_rng(alpha, nu, ns);
  x = skew_normal_rng(alpha, nu, u);
  x = skew_normal_rng(alpha, nu, 3);
  x = skew_normal_rng(alpha, nu, 3.5);
  x = skew_normal_rng(alpha, ns, alpha);
  x = skew_normal_rng(alpha, ns, nu);
  x = skew_normal_rng(alpha, ns, ns);
  x = skew_normal_rng(alpha, ns, u);
  x = skew_normal_rng(alpha, ns, 3);
  x = skew_normal_rng(alpha, ns, 3.5);
  x = skew_normal_rng(alpha, u, alpha);
  x = skew_normal_rng(alpha, u, nu);
  x = skew_normal_rng(alpha, u, ns);
  x = skew_normal_rng(alpha, u, u);
  x = skew_normal_rng(alpha, u, 3);
  x = skew_normal_rng(alpha, u, 3.5);
  x = skew_normal_rng(alpha, 3, alpha);
  x = skew_normal_rng(alpha, 3, nu);
  x = skew_normal_rng(alpha, 3, ns);
  x = skew_normal_rng(alpha, 3, u);
  x = skew_normal_rng(alpha, 3, 3);
  x = skew_normal_rng(alpha, 3, 3.5);
  x = skew_normal_rng(alpha, 3.5, alpha);
  x = skew_normal_rng(alpha, 3.5, nu);
  x = skew_normal_rng(alpha, 3.5, ns);
  x = skew_normal_rng(alpha, 3.5, u);
  x = skew_normal_rng(alpha, 3.5, 3);
  x = skew_normal_rng(alpha, 3.5, 3.5);
  x = skew_normal_rng(nu, alpha, alpha);
  x = skew_normal_rng(nu, alpha, nu);
  x = skew_normal_rng(nu, alpha, ns);
  x = skew_normal_rng(nu, alpha, u);
  x = skew_normal_rng(nu, alpha, 3);
  x = skew_normal_rng(nu, alpha, 3.5);
  x = skew_normal_rng(nu, nu, alpha);
  x = skew_normal_rng(nu, nu, nu);
  x = skew_normal_rng(nu, nu, ns);
  x = skew_normal_rng(nu, nu, u);
  x = skew_normal_rng(nu, nu, 3);
  x = skew_normal_rng(nu, nu, 3.5);
  x = skew_normal_rng(nu, ns, alpha);
  x = skew_normal_rng(nu, ns, nu);
  x = skew_normal_rng(nu, ns, ns);
  x = skew_normal_rng(nu, ns, u);
  x = skew_normal_rng(nu, ns, 3);
  x = skew_normal_rng(nu, ns, 3.5);
  x = skew_normal_rng(nu, u, alpha);
  x = skew_normal_rng(nu, u, nu);
  x = skew_normal_rng(nu, u, ns);
  x = skew_normal_rng(nu, u, u);
  x = skew_normal_rng(nu, u, 3);
  x = skew_normal_rng(nu, u, 3.5);
  x = skew_normal_rng(nu, 3, alpha);
  x = skew_normal_rng(nu, 3, nu);
  x = skew_normal_rng(nu, 3, ns);
  x = skew_normal_rng(nu, 3, u);
  x = skew_normal_rng(nu, 3, 3);
  x = skew_normal_rng(nu, 3, 3.5);
  x = skew_normal_rng(nu, 3.5, alpha);
  x = skew_normal_rng(nu, 3.5, nu);
  x = skew_normal_rng(nu, 3.5, ns);
  x = skew_normal_rng(nu, 3.5, u);
  x = skew_normal_rng(nu, 3.5, 3);
  x = skew_normal_rng(nu, 3.5, 3.5);
  x = skew_normal_rng(ns, alpha, alpha);
  x = skew_normal_rng(ns, alpha, nu);
  x = skew_normal_rng(ns, alpha, ns);
  x = skew_normal_rng(ns, alpha, u);
  x = skew_normal_rng(ns, alpha, 3);
  x = skew_normal_rng(ns, alpha, 3.5);
  x = skew_normal_rng(ns, nu, alpha);
  x = skew_normal_rng(ns, nu, nu);
  x = skew_normal_rng(ns, nu, ns);
  x = skew_normal_rng(ns, nu, u);
  x = skew_normal_rng(ns, nu, 3);
  x = skew_normal_rng(ns, nu, 3.5);
  x = skew_normal_rng(ns, ns, alpha);
  x = skew_normal_rng(ns, ns, nu);
  x = skew_normal_rng(ns, ns, ns);
  x = skew_normal_rng(ns, ns, u);
  x = skew_normal_rng(ns, ns, 3);
  x = skew_normal_rng(ns, ns, 3.5);
  x = skew_normal_rng(ns, u, alpha);
  x = skew_normal_rng(ns, u, nu);
  x = skew_normal_rng(ns, u, ns);
  x = skew_normal_rng(ns, u, u);
  x = skew_normal_rng(ns, u, 3);
  x = skew_normal_rng(ns, u, 3.5);
  x = skew_normal_rng(ns, 3, alpha);
  x = skew_normal_rng(ns, 3, nu);
  x = skew_normal_rng(ns, 3, ns);
  x = skew_normal_rng(ns, 3, u);
  x = skew_normal_rng(ns, 3, 3);
  x = skew_normal_rng(ns, 3, 3.5);
  x = skew_normal_rng(ns, 3.5, alpha);
  x = skew_normal_rng(ns, 3.5, nu);
  x = skew_normal_rng(ns, 3.5, ns);
  x = skew_normal_rng(ns, 3.5, u);
  x = skew_normal_rng(ns, 3.5, 3);
  x = skew_normal_rng(ns, 3.5, 3.5);
  x = skew_normal_rng(u, alpha, alpha);
  x = skew_normal_rng(u, alpha, nu);
  x = skew_normal_rng(u, alpha, ns);
  x = skew_normal_rng(u, alpha, u);
  x = skew_normal_rng(u, alpha, 3);
  x = skew_normal_rng(u, alpha, 3.5);
  x = skew_normal_rng(u, nu, alpha);
  x = skew_normal_rng(u, nu, nu);
  x = skew_normal_rng(u, nu, ns);
  x = skew_normal_rng(u, nu, u);
  x = skew_normal_rng(u, nu, 3);
  x = skew_normal_rng(u, nu, 3.5);
  x = skew_normal_rng(u, ns, alpha);
  x = skew_normal_rng(u, ns, nu);
  x = skew_normal_rng(u, ns, ns);
  x = skew_normal_rng(u, ns, u);
  x = skew_normal_rng(u, ns, 3);
  x = skew_normal_rng(u, ns, 3.5);
  x = skew_normal_rng(u, u, alpha);
  x = skew_normal_rng(u, u, nu);
  x = skew_normal_rng(u, u, ns);
  x = skew_normal_rng(u, u, u);
  x = skew_normal_rng(u, u, 3);
  x = skew_normal_rng(u, u, 3.5);
  x = skew_normal_rng(u, 3, alpha);
  x = skew_normal_rng(u, 3, nu);
  x = skew_normal_rng(u, 3, ns);
  x = skew_normal_rng(u, 3, u);
  x = skew_normal_rng(u, 3, 3);
  x = skew_normal_rng(u, 3, 3.5);
  x = skew_normal_rng(u, 3.5, alpha);
  x = skew_normal_rng(u, 3.5, nu);
  x = skew_normal_rng(u, 3.5, ns);
  x = skew_normal_rng(u, 3.5, u);
  x = skew_normal_rng(u, 3.5, 3);
  x = skew_normal_rng(u, 3.5, 3.5);
  x = skew_normal_rng(3, alpha, alpha);
  x = skew_normal_rng(3, alpha, nu);
  x = skew_normal_rng(3, alpha, ns);
  x = skew_normal_rng(3, alpha, u);
  x = skew_normal_rng(3, alpha, 3);
  x = skew_normal_rng(3, alpha, 3.5);
  x = skew_normal_rng(3, nu, alpha);
  x = skew_normal_rng(3, nu, nu);
  x = skew_normal_rng(3, nu, ns);
  x = skew_normal_rng(3, nu, u);
  x = skew_normal_rng(3, nu, 3);
  x = skew_normal_rng(3, nu, 3.5);
  x = skew_normal_rng(3, ns, alpha);
  x = skew_normal_rng(3, ns, nu);
  x = skew_normal_rng(3, ns, ns);
  x = skew_normal_rng(3, ns, u);
  x = skew_normal_rng(3, ns, 3);
  x = skew_normal_rng(3, ns, 3.5);
  x = skew_normal_rng(3, u, alpha);
  x = skew_normal_rng(3, u, nu);
  x = skew_normal_rng(3, u, ns);
  x = skew_normal_rng(3, u, u);
  x = skew_normal_rng(3, u, 3);
  x = skew_normal_rng(3, u, 3.5);
  x = skew_normal_rng(3, 3, alpha);
  x = skew_normal_rng(3, 3, nu);
  x = skew_normal_rng(3, 3, ns);
  x = skew_normal_rng(3, 3, u);
  z = skew_normal_rng(3, 3, 3);
  z = skew_normal_rng(3, 3, 3.5);
  x = skew_normal_rng(3, 3.5, alpha);
  x = skew_normal_rng(3, 3.5, nu);
  x = skew_normal_rng(3, 3.5, ns);
  x = skew_normal_rng(3, 3.5, u);
  z = skew_normal_rng(3, 3.5, 3);
  z = skew_normal_rng(3, 3.5, 3.5);
  x = skew_normal_rng(3.5, alpha, alpha);
  x = skew_normal_rng(3.5, alpha, nu);
  x = skew_normal_rng(3.5, alpha, ns);
  x = skew_normal_rng(3.5, alpha, u);
  x = skew_normal_rng(3.5, alpha, 3);
  x = skew_normal_rng(3.5, alpha, 3.5);
  x = skew_normal_rng(3.5, nu, alpha);
  x = skew_normal_rng(3.5, nu, nu);
  x = skew_normal_rng(3.5, nu, ns);
  x = skew_normal_rng(3.5, nu, u);
  x = skew_normal_rng(3.5, nu, 3);
  x = skew_normal_rng(3.5, nu, 3.5);
  x = skew_normal_rng(3.5, ns, alpha);
  x = skew_normal_rng(3.5, ns, nu);
  x = skew_normal_rng(3.5, ns, ns);
  x = skew_normal_rng(3.5, ns, u);
  x = skew_normal_rng(3.5, ns, 3);
  x = skew_normal_rng(3.5, ns, 3.5);
  x = skew_normal_rng(3.5, u, alpha);
  x = skew_normal_rng(3.5, u, nu);
  x = skew_normal_rng(3.5, u, ns);
  x = skew_normal_rng(3.5, u, u);
  x = skew_normal_rng(3.5, u, 3);
  x = skew_normal_rng(3.5, u, 3.5);
  x = skew_normal_rng(3.5, 3, alpha);
  x = skew_normal_rng(3.5, 3, nu);
  x = skew_normal_rng(3.5, 3, ns);
  x = skew_normal_rng(3.5, 3, u);
  z = skew_normal_rng(3.5, 3, 3);
  z = skew_normal_rng(3.5, 3, 3.5);
  x = skew_normal_rng(3.5, 3.5, alpha);
  x = skew_normal_rng(3.5, 3.5, nu);
  x = skew_normal_rng(3.5, 3.5, ns);
  x = skew_normal_rng(3.5, 3.5, u);
  z = skew_normal_rng(3.5, 3.5, 3);
  z = skew_normal_rng(3.5, 3.5, 3.5);
  
  x = student_t_rng(alpha, alpha, alpha);
  x = student_t_rng(alpha, alpha, nu);
  x = student_t_rng(alpha, alpha, ns);
  x = student_t_rng(alpha, alpha, u);
  x = student_t_rng(alpha, alpha, 3);
  x = student_t_rng(alpha, alpha, 3.5);
  x = student_t_rng(alpha, nu, alpha);
  x = student_t_rng(alpha, nu, nu);
  x = student_t_rng(alpha, nu, ns);
  x = student_t_rng(alpha, nu, u);
  x = student_t_rng(alpha, nu, 3);
  x = student_t_rng(alpha, nu, 3.5);
  x = student_t_rng(alpha, ns, alpha);
  x = student_t_rng(alpha, ns, nu);
  x = student_t_rng(alpha, ns, ns);
  x = student_t_rng(alpha, ns, u);
  x = student_t_rng(alpha, ns, 3);
  x = student_t_rng(alpha, ns, 3.5);
  x = student_t_rng(alpha, u, alpha);
  x = student_t_rng(alpha, u, nu);
  x = student_t_rng(alpha, u, ns);
  x = student_t_rng(alpha, u, u);
  x = student_t_rng(alpha, u, 3);
  x = student_t_rng(alpha, u, 3.5);
  x = student_t_rng(alpha, 3, alpha);
  x = student_t_rng(alpha, 3, nu);
  x = student_t_rng(alpha, 3, ns);
  x = student_t_rng(alpha, 3, u);
  x = student_t_rng(alpha, 3, 3);
  x = student_t_rng(alpha, 3, 3.5);
  x = student_t_rng(alpha, 3.5, alpha);
  x = student_t_rng(alpha, 3.5, nu);
  x = student_t_rng(alpha, 3.5, ns);
  x = student_t_rng(alpha, 3.5, u);
  x = student_t_rng(alpha, 3.5, 3);
  x = student_t_rng(alpha, 3.5, 3.5);
  x = student_t_rng(nu, alpha, alpha);
  x = student_t_rng(nu, alpha, nu);
  x = student_t_rng(nu, alpha, ns);
  x = student_t_rng(nu, alpha, u);
  x = student_t_rng(nu, alpha, 3);
  x = student_t_rng(nu, alpha, 3.5);
  x = student_t_rng(nu, nu, alpha);
  x = student_t_rng(nu, nu, nu);
  x = student_t_rng(nu, nu, ns);
  x = student_t_rng(nu, nu, u);
  x = student_t_rng(nu, nu, 3);
  x = student_t_rng(nu, nu, 3.5);
  x = student_t_rng(nu, ns, alpha);
  x = student_t_rng(nu, ns, nu);
  x = student_t_rng(nu, ns, ns);
  x = student_t_rng(nu, ns, u);
  x = student_t_rng(nu, ns, 3);
  x = student_t_rng(nu, ns, 3.5);
  x = student_t_rng(nu, u, alpha);
  x = student_t_rng(nu, u, nu);
  x = student_t_rng(nu, u, ns);
  x = student_t_rng(nu, u, u);
  x = student_t_rng(nu, u, 3);
  x = student_t_rng(nu, u, 3.5);
  x = student_t_rng(nu, 3, alpha);
  x = student_t_rng(nu, 3, nu);
  x = student_t_rng(nu, 3, ns);
  x = student_t_rng(nu, 3, u);
  x = student_t_rng(nu, 3, 3);
  x = student_t_rng(nu, 3, 3.5);
  x = student_t_rng(nu, 3.5, alpha);
  x = student_t_rng(nu, 3.5, nu);
  x = student_t_rng(nu, 3.5, ns);
  x = student_t_rng(nu, 3.5, u);
  x = student_t_rng(nu, 3.5, 3);
  x = student_t_rng(nu, 3.5, 3.5);
  x = student_t_rng(ns, alpha, alpha);
  x = student_t_rng(ns, alpha, nu);
  x = student_t_rng(ns, alpha, ns);
  x = student_t_rng(ns, alpha, u);
  x = student_t_rng(ns, alpha, 3);
  x = student_t_rng(ns, alpha, 3.5);
  x = student_t_rng(ns, nu, alpha);
  x = student_t_rng(ns, nu, nu);
  x = student_t_rng(ns, nu, ns);
  x = student_t_rng(ns, nu, u);
  x = student_t_rng(ns, nu, 3);
  x = student_t_rng(ns, nu, 3.5);
  x = student_t_rng(ns, ns, alpha);
  x = student_t_rng(ns, ns, nu);
  x = student_t_rng(ns, ns, ns);
  x = student_t_rng(ns, ns, u);
  x = student_t_rng(ns, ns, 3);
  x = student_t_rng(ns, ns, 3.5);
  x = student_t_rng(ns, u, alpha);
  x = student_t_rng(ns, u, nu);
  x = student_t_rng(ns, u, ns);
  x = student_t_rng(ns, u, u);
  x = student_t_rng(ns, u, 3);
  x = student_t_rng(ns, u, 3.5);
  x = student_t_rng(ns, 3, alpha);
  x = student_t_rng(ns, 3, nu);
  x = student_t_rng(ns, 3, ns);
  x = student_t_rng(ns, 3, u);
  x = student_t_rng(ns, 3, 3);
  x = student_t_rng(ns, 3, 3.5);
  x = student_t_rng(ns, 3.5, alpha);
  x = student_t_rng(ns, 3.5, nu);
  x = student_t_rng(ns, 3.5, ns);
  x = student_t_rng(ns, 3.5, u);
  x = student_t_rng(ns, 3.5, 3);
  x = student_t_rng(ns, 3.5, 3.5);
  x = student_t_rng(u, alpha, alpha);
  x = student_t_rng(u, alpha, nu);
  x = student_t_rng(u, alpha, ns);
  x = student_t_rng(u, alpha, u);
  x = student_t_rng(u, alpha, 3);
  x = student_t_rng(u, alpha, 3.5);
  x = student_t_rng(u, nu, alpha);
  x = student_t_rng(u, nu, nu);
  x = student_t_rng(u, nu, ns);
  x = student_t_rng(u, nu, u);
  x = student_t_rng(u, nu, 3);
  x = student_t_rng(u, nu, 3.5);
  x = student_t_rng(u, ns, alpha);
  x = student_t_rng(u, ns, nu);
  x = student_t_rng(u, ns, ns);
  x = student_t_rng(u, ns, u);
  x = student_t_rng(u, ns, 3);
  x = student_t_rng(u, ns, 3.5);
  x = student_t_rng(u, u, alpha);
  x = student_t_rng(u, u, nu);
  x = student_t_rng(u, u, ns);
  x = student_t_rng(u, u, u);
  x = student_t_rng(u, u, 3);
  x = student_t_rng(u, u, 3.5);
  x = student_t_rng(u, 3, alpha);
  x = student_t_rng(u, 3, nu);
  x = student_t_rng(u, 3, ns);
  x = student_t_rng(u, 3, u);
  x = student_t_rng(u, 3, 3);
  x = student_t_rng(u, 3, 3.5);
  x = student_t_rng(u, 3.5, alpha);
  x = student_t_rng(u, 3.5, nu);
  x = student_t_rng(u, 3.5, ns);
  x = student_t_rng(u, 3.5, u);
  x = student_t_rng(u, 3.5, 3);
  x = student_t_rng(u, 3.5, 3.5);
  x = student_t_rng(3, alpha, alpha);
  x = student_t_rng(3, alpha, nu);
  x = student_t_rng(3, alpha, ns);
  x = student_t_rng(3, alpha, u);
  x = student_t_rng(3, alpha, 3);
  x = student_t_rng(3, alpha, 3.5);
  x = student_t_rng(3, nu, alpha);
  x = student_t_rng(3, nu, nu);
  x = student_t_rng(3, nu, ns);
  x = student_t_rng(3, nu, u);
  x = student_t_rng(3, nu, 3);
  x = student_t_rng(3, nu, 3.5);
  x = student_t_rng(3, ns, alpha);
  x = student_t_rng(3, ns, nu);
  x = student_t_rng(3, ns, ns);
  x = student_t_rng(3, ns, u);
  x = student_t_rng(3, ns, 3);
  x = student_t_rng(3, ns, 3.5);
  x = student_t_rng(3, u, alpha);
  x = student_t_rng(3, u, nu);
  x = student_t_rng(3, u, ns);
  x = student_t_rng(3, u, u);
  x = student_t_rng(3, u, 3);
  x = student_t_rng(3, u, 3.5);
  x = student_t_rng(3, 3, alpha);
  x = student_t_rng(3, 3, nu);
  x = student_t_rng(3, 3, ns);
  x = student_t_rng(3, 3, u);
  z = student_t_rng(3, 3, 3);
  z = student_t_rng(3, 3, 3.5);
  x = student_t_rng(3, 3.5, alpha);
  x = student_t_rng(3, 3.5, nu);
  x = student_t_rng(3, 3.5, ns);
  x = student_t_rng(3, 3.5, u);
  z = student_t_rng(3, 3.5, 3);
  z = student_t_rng(3, 3.5, 3.5);
  x = student_t_rng(3.5, alpha, alpha);
  x = student_t_rng(3.5, alpha, nu);
  x = student_t_rng(3.5, alpha, ns);
  x = student_t_rng(3.5, alpha, u);
  x = student_t_rng(3.5, alpha, 3);
  x = student_t_rng(3.5, alpha, 3.5);
  x = student_t_rng(3.5, nu, alpha);
  x = student_t_rng(3.5, nu, nu);
  x = student_t_rng(3.5, nu, ns);
  x = student_t_rng(3.5, nu, u);
  x = student_t_rng(3.5, nu, 3);
  x = student_t_rng(3.5, nu, 3.5);
  x = student_t_rng(3.5, ns, alpha);
  x = student_t_rng(3.5, ns, nu);
  x = student_t_rng(3.5, ns, ns);
  x = student_t_rng(3.5, ns, u);
  x = student_t_rng(3.5, ns, 3);
  x = student_t_rng(3.5, ns, 3.5);
  x = student_t_rng(3.5, u, alpha);
  x = student_t_rng(3.5, u, nu);
  x = student_t_rng(3.5, u, ns);
  x = student_t_rng(3.5, u, u);
  x = student_t_rng(3.5, u, 3);
  x = student_t_rng(3.5, u, 3.5);
  x = student_t_rng(3.5, 3, alpha);
  x = student_t_rng(3.5, 3, nu);
  x = student_t_rng(3.5, 3, ns);
  x = student_t_rng(3.5, 3, u);
  z = student_t_rng(3.5, 3, 3);
  z = student_t_rng(3.5, 3, 3.5);
  x = student_t_rng(3.5, 3.5, alpha);
  x = student_t_rng(3.5, 3.5, nu);
  x = student_t_rng(3.5, 3.5, ns);
  x = student_t_rng(3.5, 3.5, u);
  z = student_t_rng(3.5, 3.5, 3);
  z = student_t_rng(3.5, 3.5, 3.5);

  theta = dirichlet_rng(alpha);
  
  v = multi_normal_rng(mu, Sigma);
  v = multi_normal_rng(nu, Sigma);
  v_array = multi_normal_rng(mu_array, Sigma);
  v_array = multi_normal_rng(nu_array, Sigma);

  v = multi_normal_cholesky_rng(mu, Sigma);
  v = multi_normal_cholesky_rng(nu, Sigma);
  v_array = multi_normal_cholesky_rng(mu_array, Sigma);
  v_array = multi_normal_cholesky_rng(nu_array, Sigma);

  v = multi_student_t_rng(3.0, mu, Sigma);
  v = multi_student_t_rng(3.0, nu, Sigma);
  v_array = multi_student_t_rng(3.0, mu_array, Sigma);
  v_array = multi_student_t_rng(3.0, nu_array, Sigma);
  
  // v = multi_normal_prec_rng(mu,Sigma);
  Sigma = wishart_rng(3.0,Sigma);
  Sigma = inv_wishart_rng(3.0,Sigma);
  Sigma = lkj_corr_rng(3,2.5);
  L = lkj_corr_cholesky_rng(3,3.0);
  // DEPRECATE:  Sigma = lkj_cov_rng(Sigma, mu, alpha, 2.0);
}
