USERDIR = ../inst/libs$(R_ARCH)
STATICLIB = librstan.a
USERLIB = $(USERDIR)/$(STATICLIB) 

.PHONY: all clean

all: userlib $(SHLIB)

clean:
	rm -f $(OBJECTS) $(SHLIB)

$(USERLIB): $(OBJECTS)

userlib: $(STATICLIB)
	-mkdir -p $(USERDIR)
	-mv $(STATICLIB) $(USERLIB) 

$(STATICLIB): $(OBJECTS)

