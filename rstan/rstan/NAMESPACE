useDynLib(rstan)
# exportPattern("^[[:alpha:]]+")
export (
  stan.model,
# stan.samples,
  stanc, 
  stanc.version, 
  stan,
  stan.dump
)

exportClasses (
  stanmodel, stanfit 
) 
exportMethods (
#   print, plot, 
#   extract, 
  show, sampling, summary, print, extract, chain.summary,
  traceplot
)
import(Rcpp)
