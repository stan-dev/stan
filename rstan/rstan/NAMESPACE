useDynLib(rstan)
# exportPattern("^[[:alpha:]]+")
export (
  stan.model,
# stan.samples,
  stanc, 
  stanc.version 
)

exportClasses (
  stanmodel, stanfit 
) 
exportMethods (
#   print, plot, 
#   extract, 
  show, sampling, summary, print, extract 
)
import(Rcpp)
